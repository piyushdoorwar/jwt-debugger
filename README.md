# jwt debugger

Standalone JWT debugger UI inspired by DevUtils tools.

## Local Preview

Open `index.html` in a browser to explore the debugger, sample token, and live time helpers that mirror the DevUtils style.

